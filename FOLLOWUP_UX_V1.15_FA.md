# بازطراحی UX پیگیری‌ها در IncidentHub v1.15

این تغییر با حفظ کامل API و عملیات موجود پیگیری‌ها، صفحه «پیگیری‌ها» را برای استفاده روزمره NOC بازطراحی می‌کند.

## اصول طراحی

- چهار نمای سریع «عقب‌افتاده»، «امروز»، «آینده» و «انجام‌شده» حفظ شده و hierarchy بصری آن‌ها تقویت شده است.
- صف اصلی دارای فیلتر سریع «همه / باز / بسته»، جست‌وجو، فیلتر نوع، فیلتر مسئول و شمارنده نتیجه است.
- پیگیری‌های قدیمی و عقب‌افتاده بر اساس `staleAfterHours` به شکل «عقب‌افتاده بحرانی» متمایز می‌شوند تا در صف NOC گم نشوند.
- کارت هر پیگیری اطلاعات عملیاتی اصلی را در یک نگاه نشان می‌دهد: وضعیت، نوع، موعد، رخداد مرتبط، اقدام/نتیجه، مسئول و اولویت.
- «مشاهده جزئیات» برای همه وضعیت‌ها در دسترس است و Drawer اختصاصی شامل metadata، شرح رخداد، اقدام/نتیجه و timeline است.
- عملیات واقعی قبلی `COMPLETE`، `RESCHEDULE` و `CANCEL` بدون تغییر قرارداد API حفظ شده‌اند.
- Drawer با کلیک روی backdrop یا کلید Escape بسته می‌شود.
- چیدمان در عرض‌های کوچک به یک ستون تبدیل می‌شود و `prefers-reduced-motion` رعایت شده است.

## معیار پذیرش دستی

1. فیلترهای «همه / باز / بسته» باید بدون reload صف را صحیح محدود کنند.
2. کلیک روی چهار کارت خلاصه باید نمای مربوط را فعال کند و فیلتر وضعیت صف را reset کند.
3. جست‌وجو باید شناسه رخداد، موضوع، مسئول، نوع، اقدام و نتیجه را پوشش دهد.
4. پیگیری عقب‌افتاده بیش از آستانه تنظیم‌شده باید «عقب‌افتاده بحرانی» نمایش داده شود.
5. Drawer جزئیات باید از تمام کارت‌ها باز شود و با Escape بسته شود.
6. «باز کردن رخداد» باید Drawer پیگیری را ببندد و Incident مرتبط را باز کند.
7. ثبت نتیجه، تغییر زمان و لغو باید همان APIهای قبلی را استفاده کنند و regression نداشته باشند.
8. در موبایل/عرض کم، کارت‌ها، toolbar و Drawer نباید overflow افقی ایجاد کنند.
